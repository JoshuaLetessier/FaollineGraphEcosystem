using UnityEngine;

namespace Faolline.GraphCore.Editor
{
    /// <summary>Shared constants for the GraphCore editor layer.</summary>
    public static class GraphCoreDefaults
    {
        /// <summary>Fallback node/edge color when no override or type color is registered.</summary>
        public static readonly Color NodeGrey = new Color(0.502f, 0.502f, 0.502f);
    }
}
