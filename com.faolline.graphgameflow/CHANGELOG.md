# Changelog

All notable changes to **com.faolline.graphgameflow** are documented here.
The format is based on [Keep a Changelog](https://keepachangelog.com/) and the project uses
[Semantic Versioning](https://semver.org/).

## [0.1.0] — unreleased

### Added
- Initial release of the orchestrator / host layer above `com.faolline.graphcore`.
- _(filled at finalize — task T018: host bridge `GraphFlowDriver`, `LoadSceneAction`, `ISceneLoader` seam,
  `GameFlowContext`, the Linear reference scene-flow.)_
